# Scout Agent Kit

Contains:
- `IDENTITY.md`
- `SOUL.md`
- `AGENTS.md`

## Wiring steps
1. Point the `scout` agent entry in `~/.openclaw/openclaw.json` to this workspace (`.../workspace-inkkeeper/agents/scout`).
2. Restart the OpenClaw gateway.
3. Clear existing Scout sessions so the research channel boots with the new persona.
4. Smoke-test with a “who are you?” prompt.

Add tool/site notes to `TOOLS.md` (APIs, paywalled sources, etc.) as the lane matures.